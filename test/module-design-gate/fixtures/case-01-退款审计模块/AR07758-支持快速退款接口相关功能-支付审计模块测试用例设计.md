# AR07758-支持快速退款接口相关功能-支付审计模块测试用例设计

> ⚠ 本文件是**测试 fixture**，由 module-test-design 阶段产出（模拟）。它本身大体合法，但**故意只覆盖主路径，漏掉关键失败路径和风险**，配合说明书的可测试性输入不足，用于验证 gate 的"验证闭环性"维度能否抓出来。

## 1. 测试范围与策略

| 项目 | 内容 |
|---|---|
| 目标模块 | 支付审计模块 |
| 输入正式说明书 | AR07758-支持快速退款接口相关功能-支付审计模块详细设计说明书.md |
| 输入 context | AR07758-支持快速退款接口相关功能-支付审计模块详细设计说明书.context.md |
| 测试设计状态 | 完成 |
| 策略摘要 | 采用最小充分验证集，优先覆盖 P0/P1 设计风险，允许一条用例覆盖多个设计目标。 |
| 阻塞或待补设计摘要 | 无 |

## 2. 最小充分测试用例集

| 用例编号 | 优先级 | 测试层级 | 覆盖目标 | 场景 | 建议位置 | 前置条件 / Fixture / Mock | 输入 / 触发步骤 | 预期结果 | 断言方式 / 观察点 | 自动化方式 |
|---|---|---|---|---|---|---|---|---|---|---|
| TC1 | P0 | 单元 | R1 主路径 | 正常退款事件写入 | src/payment-audit/test/AuditRecorderTest.java | mock audit_log | 发送退款事件 | 写入成功 | audit_log 含记录 | JUnit5 |
| TC2 | P0 | 单元 | R2 主路径 | 按 refundId 查询 | src/payment-audit/test/AuditQueryServiceTest.java | mock 数据 | 查询 refundId | 返回记录 | 返回非空列表 | JUnit5 |
| TC3 | P1 | 契约 | K1 | GET 接口返回结构 | src/payment-audit/test/AuditControllerTest.java | - | GET 请求 | 200 | 返回结构符合契约 | JUnit5 |

> ⚠ 配合说明书植入的缺陷，本测试设计**故意遗漏**：
> - 退款写入失败的失败路径用例
> - 订单更新失败的回滚用例（D3 相关）
> - 退款幂等性用例（A3 相关，说明书把它当事实了，这里也无对应验证）
> - 契约 K2（RefundAuditEvent DTO）字段一致性用例
>
> gate 的"验证闭环性"检查应判定这些 P0/P1 目标未被覆盖，给出 GAP 回流建议。

## 3. 覆盖矩阵

| TOBE 决策 / 契约 / 风险 | 优先级 | 覆盖用例 | 覆盖状态 | 未覆盖原因 |
|---|---|---|---|---|
| D1 主路径 | P0 | TC1 | 已覆盖 | |
| D2 查询主路径 | P0 | TC2 | 已覆盖 | |
| K1 接口契约 | P1 | TC3 | 已覆盖 | |
| D3 订单更新 | P0 | （无） | 未覆盖 | 缺失败路径和回滚用例 |
| 退款幂等 | P0 | （无） | 未覆盖 | 说明书未明确，无法设计用例 |
| K2 DTO 契约 | P1 | （无） | 未覆盖 | 字段在说明书中不一致，无法断言 |

## 4. 缺口与回流建议

> ⚠ 本节**故意留空**。按照 module-test-design SKILL.md，未覆盖的 P0/P1 目标必须写入缺口表并给出回流阶段（回 TOBE / 回 ASIS / 上游确认）。这里留空会让 gate 发现：测试设计未把已知缺口显性化。

| 缺口编号 | 缺口类型 | 关联 TOBE 决策 / 契约 / 风险 | 缺口描述 | 影响 | 回流阶段 | 补齐后通过条件 |
|---|---|---|---|---|---|---|

## 5. 变更记录

| 轮次 | 变更摘要 | 来源 |
|---|---|---|
| 第 1 轮 | 初始生成 | fixture |
